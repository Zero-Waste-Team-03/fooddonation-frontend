import { PageWrapper } from "@/components/layout/PageWrapper";

export function ModerationPage() {
  return (
    <PageWrapper
      title="Moderation"
      description="No matching frame on the web canvas; shell only."
    >
      <p className="text-sm text-muted-foreground">Moderation workspace integration pending.</p>
    </PageWrapper>
  );
}
