mutation RefreshTokens {
  refreshTokens {
    accessToken
    refreshToken
    user {
      id
      email
      displayName
      role
      status
      isMailVerified
      avatar {
        id
        url
      }
    }
  }
}
