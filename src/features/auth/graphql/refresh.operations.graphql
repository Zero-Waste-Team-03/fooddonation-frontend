mutation RefreshTokens {
  refreshTokens {
    accessToken
    refreshToken
    user {
      id
      email
      displayName
      role
      status
      isFoodSaver
      isVerified
      avatar {
        id
        url
      }
    }
  }
}
