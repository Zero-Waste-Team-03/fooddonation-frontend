mutation RefreshTokens {
  refreshTokens {
    accessToken
    refreshToken
    user {
      id
      email
      displayName
      role
      status
      isVerified
      avatar {
        id
        url
      }
    }
  }
}
